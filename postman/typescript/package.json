{
  "name": "gatling-ecomm-postman-ts",
  "version": "3.15.2",
  "private": true,
  "main": "target/bundle",
  "dependencies": {
    "@gatling.io/core": "3.15.2",
    "@gatling.io/http": "3.15.2",
    "@gatling.io/postman": "3.15.2"
  },
  "devDependencies": {
    "@gatling.io/cli": "3.15.2",
    "prettier": "3.8.3",
    "rimraf": "6.1.3",
    "typescript": "6.0.2"
  },
  "scripts": {
    "clean": "rimraf target",
    "format": "prettier --write \"**/*.ts\"",
    "check": "tsc --noEmit",
    "build": "tsc --noEmit && gatling build --typescript",
    "basicSimulation": "tsc --noEmit && gatling run --typescript --simulation basicSimulation",
    "advancedSimulation": "tsc --noEmit && gatling run --typescript --simulation advancedSimulation"
  }
}
